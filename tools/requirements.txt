# Dependances de l outillage ROS de ce depot (serveurs MCP ros2-*).
#
# UNE seule ligne, et c est le point : ces serveurs ne parlent au robot que par
# rosbridge (WebSocket JSON). Pas de pyserial, pas de rclpy -- le port serie est
# mono-proprietaire et appartient au driver, et ROS ne tourne pas sur cet hote Windows.
#   py -3 -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt
websocket-client>=1.6
